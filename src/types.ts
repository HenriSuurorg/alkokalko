export type drinkType = {
  type: string;
  volume: string;
  unit: string;
  ABV: string;
  timePassed: string;
};

export type duplicateDrinkType = {
  type: string;
  volume: string;
  unit: string;
  ABV: string;
};
